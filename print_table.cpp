#include <bits/stdc++.h>
using namespace std;
void UpperCase(vector<char> &arr)
{
    for (char &x : arr)
    {
        x = x - 32;
    }
}
int main(int argc, char *argv[])
{
    // string str = argv[1];
    // int count = 0;
    // vector<char> vowels = {'a', 'e', 'i', 'o', 'u'};
    // for (auto it : vowels)
    // {
    //     for (auto x : str)
    //     {
    //         if (x == it)
    //         {
    //             count++;
    //         }
    //     }
    // }
    // cout << "The number of vowels is " << count << endl;
    return 0;
}